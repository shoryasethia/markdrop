# Python API Reference

Markdrop is not just a CLI tool; it provides a robust, strongly-typed internal Python API. Both the document extraction and AI generation features are designed to be imported cleanly into automated backend pipelines, Jupyter Notebooks, or enterprise RAG ingestion loops.

---

## 1. Class: `MarkDropConfig`
Holds the operational settings for the initial PDF parsing, image extraction, and HTML rendering step.

```python
from markdrop import MarkDropConfig
import logging

config = MarkDropConfig(
    # How sharp the extracted pixel matrices are. Higher means larger files but better AI Vision input.
    image_resolution_scale=2.0,
    # Customization for the interactive Web application viewer
    download_button_color="#444444",
    # Internal module structure logging. Does not leak into your primary application logger.
    log_level=logging.INFO,
    log_dir="logs",
    # Where temporary `.xlsx` files generated by `add_downloadable_tables` are dumped
    excel_dir="markdrop_excel_tables",
)
```

---

## 2. Function: `markdrop()`
The main entry point for PDF extraction logic. Evaluates a binary PDF and converts the unstructured text format into a structured Markdown graph with associated extracted images.

```python
from markdrop import markdrop

# You can pass both local file paths and https:// URLs (SSRF-checked download)
html_path = markdrop(
    input_doc_path="path/to/research_paper.pdf", output_dir="/data/dump", config=config
)

print(f"Generated Web Viewer at: {html_path}")
```
*   **Returns:** A `Path` object pointing to the newly generated `research_paper-markdroped.html`. It simultaneously generates `research_paper-markdroped.md` inside that same directory.

---

## 3. Function: `add_downloadable_tables()`
An optional augmentation to `markdrop()`. Evaluates the generated HTML, extracts all `<table>` elements into `pandas` dataframes, builds standalone Excel documents containing them, and dynamically updates the HTML to feature UI download buttons.

```python
from markdrop import add_downloadable_tables

html_string_modified = add_downloadable_tables(html_path, config)
```

---

## 4. Class: `ProcessorConfig`
Holds the operational instructions defining how AI enrichment should act on an existing Markdown file. Default model names match `markdrop/parse.py`.

```python
from markdrop import ProcessorConfig, AIProvider

config = ProcessorConfig(
    # Target file
    input_path="data_dumps/research_paper-markdroped.md",
    output_dir="data_dumps/ai_enriched",
    # Primary routing interface
    ai_provider=AIProvider.GEMINI,
    # Model parameters — defaults from parse.py (e.g. gemini-3.1-flash-lite).
    # Override per provider or use model_name_override / text_model_name_override.
    gemini_model_name="gemini-3.1-flash-lite",
    # Feature Toggles
    # When True, the ![alt text](images/123.jpg) syntax is DELETED entirely, replaced by AI text.
    # When False, the AI text is merely appended beneath the image syntax in the Markdown doc.
    remove_images=False,
    # True to parse and summarize tabular data.
    table_descriptions=True,
    # Fault Tolerance in cases of Cloud Model rate-limiting
    max_retries=3,
    retry_delay=2,
)
```

---

## 5. Coroutine: `process_markdown()`

`process_markdown` is an **async coroutine**. It reads a Markdown file, finds embedded images and tables, and dispatches AI requests concurrently (bounded by `max_concurrency`).

To use `process_markdown` in a Python script, wrap it in an `asyncio` event loop:

```python
import asyncio
from markdrop import process_markdown, ProcessorConfig, AIProvider

config = ProcessorConfig(
    input_path="data_dumps/research_paper-markdroped.md",
    output_dir="data_dumps/ai_enriched",
    ai_provider=AIProvider.OPENAI,
)


async def build_pipeline():
    enriched_file = await process_markdown(config)
    print(f"Successfully digested AI models into: {enriched_file}")


asyncio.run(build_pipeline())
```

The returned path points to `{stem}_processed.md` in `output_dir`.

---

## 6. Helper: `setup_keys()`
A programmatic interface equivalent to the `markdrop setup` CLI command. Saves keys to the user config `.env` file.

```python
from markdrop import setup_keys

# Manually trigger the interactive CLI prompt
setup_keys("gemini")
```

API keys are loaded automatically from the user config directory when `process_markdown` initializes its AI client.

---

## 7. Helper: `generate_descriptions()`
A function exposing the underlying `generate_response()` model routing interface across batches of files directly. Useful if you want to skip PDF parsing entirely and simply want an AI to analyze local images against a unified prompt.

```python
from markdrop import generate_descriptions

csv_output_path = generate_descriptions(
    input_path="/datasets/images/batch_1",
    output_dir="/datasets/analysis/",
    prompt="Identify the exact SKU serial number printed on the device. Provide the SKU, or say 'UNKNOWN'.",
    # You can pass an array of providers to benchmark outputs against the EXACT image and prompt in the resulting CSV
    llm_client=["gemini", "anthropic", "qwen", "molmo"],
)
```
